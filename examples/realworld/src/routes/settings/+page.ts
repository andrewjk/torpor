import component from "@/views/settings/Settings.torp";
import { type PageEndPoint } from "@torpor/build";

export default {
	component,
	head: [{ title: "Settings • Conduit" }],
} satisfies PageEndPoint;
