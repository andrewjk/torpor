import { Toggle } from "../../../src/Form/index";

export default function ToggleTest($props: any) {
	@render {
		<Toggle
			{$props?.name}
			{$props?.checked}
			{$props?.disabled}
			ariaLabel={$props?.ariaLabel ?? "Notifications"}
			onchange={$props?.onchange}
		/>
	}
}
