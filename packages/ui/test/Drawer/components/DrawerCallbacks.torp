import { Drawer, DrawerContent, DrawerOverlay, DrawerTrigger } from "../../../src/Drawer";

export default function DrawerCallbacks() {
	@render {
		<Drawer {$props?.ontoggle} {$props?.onopen} {$props?.onclose}>
			<DrawerTrigger>Open Drawer</DrawerTrigger>
			<DrawerOverlay />
			<DrawerContent ariaLabel="Callback Drawer">
				<h2>Callback Test</h2>
				<p>Check console for callback output.</p>
			</DrawerContent>
		</Drawer>
	}
}
