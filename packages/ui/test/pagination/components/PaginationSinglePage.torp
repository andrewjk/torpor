import {
	Pagination,
	PaginationItem,
} from "../../../src/Pagination/index";

export default function PaginationSinglePage() {
	let $state = $watch({ page: 1 });

	@render {
		<Pagination count={1} page={$state.page}>
			<PaginationItem page="start" active={$state.page === 1}>«</PaginationItem>
			<PaginationItem page="previous" active={$state.page === 1}>‹</PaginationItem>
			@for (let page of $slot.pages) {
				<PaginationItem page={page} active={$state.page === page}>
					{page}
				</PaginationItem>
			}
			<PaginationItem page="next" active={$state.page === 1}>›</PaginationItem>
			<PaginationItem page="end" active={$state.page === 1}>»</PaginationItem>
		</Pagination>
	}
}
