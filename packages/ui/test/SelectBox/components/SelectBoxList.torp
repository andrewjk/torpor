import {
	ListBox,
	ListBoxItem,
} from "../../../src/ListBox/index";
import {
	SelectBox,
	SelectBoxContent,
	SelectBoxTrigger,
} from "../../../src/SelectBox/index";

export default function ListBoxList($props: any) {
	@render {
		<SelectBox &value={$props.value} name="hi">
			<SelectBoxTrigger />
			<SelectBoxContent>
				@// TODO: Shouldn't have to set value here:
				<ListBox value={$props.value}>
					@for (let item of $props.items) {
						@key = item.id
						<ListBoxItem value={item.id}>
							{item.text}
						</ListBoxItem>
					}
				</ListBox>
			</SelectBoxContent>
		</SelectBox>
	}
}
