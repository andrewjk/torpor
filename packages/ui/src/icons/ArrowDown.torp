import { type IconProps } from "./IconTypes";

export default function ArrowDown($props: IconProps | undefined) {
	@render {
		<svg
			{$props?.id}
			class={`torp-icon ${$props?.class ?? ""}`.trim()}
			viewBox="0 0 100 100"
			stroke-linejoin="round"
			stroke-width="12"
		>
			<g>
				<polygon points="25,40 50,70 75,40" />
			</g>
		</svg>
	}
}
