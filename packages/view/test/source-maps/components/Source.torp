export default function Source($props: { counter: number }) {
const x = 5;
console.log(x);

	@render {
		@if ($props.counter > 10) {
			<p>
				It's large.
			</p>
		} else {
			<p>
				It's small.
			</p>
		}
	}

const y = 10;
console.log(y);
}
