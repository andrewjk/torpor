export default function NestedIf($props: { condition: boolean, counter: number }) {
	@render {
		@if ($props.condition) {
			@if ($props.counter > 5) {
				<p>It's big</p>
			} else {
				<p>It's small</p>
			}
		}
	}
}
