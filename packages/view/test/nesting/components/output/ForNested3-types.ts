import type SlotRender from "../../../../src/types/SlotRender";

declare function ForNested3(
	$parent: ParentNode,
	$anchor: Node | null,
	$props: { matrix: number[][]; highlight: number },
	$context?: Record<PropertyKey, any>,
	$slots?: Record<string, SlotRender>,
): void;
export default ForNested3;
