export type OperationType =
	| "@if group"
	| "@if"
	| "@else if"
	| "@else"
	| "@switch group"
	| "@case"
	| "@default"
	| "@for group"
	| "@for"
	| "@key"
	| "@await group"
	| "@await"
	| "@then"
	| "@catch"
	| "@replace group"
	| "@replace"
	| "@html group"
	| "@html"
	| "@const"
	| "@console"
	| "@debugger"
	| "@function";
