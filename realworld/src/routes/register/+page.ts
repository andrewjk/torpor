import component from "@/components/account/Register.torp";
import type { PageEndPoint } from "@torpor/build";

export default {
	component,
	head: [{ title: "Sign up • Conduit" }],
} satisfies PageEndPoint;
