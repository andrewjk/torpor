{
	"name": "@torpor/site",
	"version": "0.0.1",
	"description": "The home and documentation site for Torpor",
	"type": "module",
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"dev": "cd ../view && pnpm build && cd ../unplugin && pnpm build && cd ../build && pnpm build && cd ../site && tb --dev",
		"build": "tb --config torpor.config.js --build",
		"preview": "vinxi serve"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@torpor/ui": "workspace:^",
		"@torpor/view": "workspace:^",
		"sucrase": "^3.35.0"
	},
	"devDependencies": {
		"@torpor/build": "workspace:^",
		"@torpor/unplugin": "workspace:^",
		"vinxi": "^0.5.1"
	}
}
