import { TabGroup, TabList, TabTrigger, TabHeader, TabContent } from "@torpor/ui/TabGroup"

import ExampleTabGroup from "./ExampleTabGroup.torp";

import ExampleBasic from "./pagination/ExampleBasic.torp";

import ExampleBasicCode from "./pagination/ExampleBasic.torp?raw";

export default function PaginationPage() {
	@head {
		<title>Tool Bar | Torpor</title>
	}

	@render {
		<h1>Tool Bar</h1>
		<p>
			Buttons with page numbers that can be used to move between pages.
		</p>
		<h2>Structure</h2>
		<pre><code>{`
<Pagination>
	<PaginationItem />
	...
</Pagination>
`.trim()}</code></pre>
		<h2>Demo</h2>
		<TabGroup>
			<TabList ariaLabel="Example tabs">
				<TabHeader>
					<TabTrigger class="example-tab-header">
						Basic
					</TabTrigger>
				</TabHeader>
			</TabList>
			<TabContent>
				<ExampleTabGroup>
					<ExampleBasic />
					<fill name="code">
						<pre><code>{ExampleBasicCode.trim()}</code></pre>
					</fill>
				</ExampleTabGroup>
			</TabContent>
		</TabGroup>
	}
}
