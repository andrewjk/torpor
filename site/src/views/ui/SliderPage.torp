import { TabContent, TabGroup, TabList } from "@torpor/ui/TabGroup";
import CodePreview from "../CodePreview.torp";
import ExampleTabGroup from "./ExampleTabGroup.torp";
import ExampleTabHeader from "./ExampleTabHeader.torp";
import ExampleBasic from "./slider/ExampleBasic.torp";
import ExampleBasicCode from "./slider/ExampleBasic.torp?raw";
import ExampleComposed from "./slider/ExampleComposed.torp";
import ExampleComposedCode from "./slider/ExampleComposed.torp?raw";
import ExampleRange from "./slider/ExampleRange.torp";
import ExampleRangeCode from "./slider/ExampleRange.torp?raw";

export default function SliderPage() {
	@head {
		<title>Slider | Torpor</title>
	}

	@render {
		<h1>Slider</h1>
		<p>
			An input where the user selects a value from within a given range, by dragging a thumb
			along a track or by using the keyboard.
		</p>
		<p>
			See{" "}
			<a href="https://www.w3.org/WAI/ARIA/apg/patterns/slider/">
				the WAI ARIA guidelines for Sliders
			</a>
			.
		</p>
		<h2>Structure</h2>
		<pre><code>{`
<Slider>
	<SliderRange />
	<SliderHandle />
</Slider>
`.trim()}</code></pre>
		<p>
			With no children, the range and handle are rendered automatically.
			Compose them explicitly to target each one with its own props and
			styling.
		</p>
		<h2>Demo</h2>
		<TabGroup>
			<TabList ariaLabel="Example tabs">
				<ExampleTabHeader>
					Basic
				</ExampleTabHeader>
				<ExampleTabHeader>
					Range
				</ExampleTabHeader>
				<ExampleTabHeader>
					Composed
				</ExampleTabHeader>
			</TabList>
			<TabContent>
				<ExampleTabGroup>
					<ExampleBasic />
					<fill name="code">
						<CodePreview code={ExampleBasicCode} />
					</fill>
				</ExampleTabGroup>
			</TabContent>
			<TabContent>
				<ExampleTabGroup>
					<ExampleRange />
					<fill name="code">
						<CodePreview code={ExampleRangeCode} />
					</fill>
				</ExampleTabGroup>
			</TabContent>
			<TabContent>
				<ExampleTabGroup>
					<ExampleComposed />
					<fill name="code">
						<CodePreview code={ExampleComposedCode} />
					</fill>
				</ExampleTabGroup>
			</TabContent>
		</TabGroup>
	}
}
