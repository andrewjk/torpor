import SectionLayout from "../SectionLayout.torp";

export default function BuildLayout() {
	@render {
		<SectionLayout links={[{ label: "Overview", href: "/build" }]}>
			<slot />
		</SectionLayout>
	}
}
