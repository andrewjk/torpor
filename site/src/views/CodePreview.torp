import { codeToHtml } from "@torpor/shiki";

/**
 * Displays a colorized code preview.
 */
export default function CodePreview($props) {
	@head {
		<title>Torpor</title>
	}

	let $state = $watch({
		// Format the code asynchronously; re-formats when $props.code changes.
		get formatted() {
			return $async(async () => {
				const code = $props.code;
				return (await codeToHtml(code, {
					lang: "torpor",
					theme: "light-plus",
				})).replace(/background-color:\s*#[a-f\d]{6};\s*/i, "");
			});
		},
	});

	@render {
		@await {
			@html($state.formatted)
		} with {
			<pre><code>{$props.code}</code></pre>
		}
	}
}
