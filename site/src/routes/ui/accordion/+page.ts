import component from "@/components/ui/Accordion.torp";
import { type PageEndPoint } from "@torpor/build";
import { Accordion } from "@torpor/ui/Accordion";

export default {
	component,
} satisfies PageEndPoint;
