{
  "name": "preact-jsbench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "js-framework-benchmark fixture for Preact (keyed hooks implementation).",
  "scripts": {
    "dev": "vp dev --host --port 5260 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5260 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default",
    "vite-plus": "catalog:"
  }
}
