{
	"name": "torpor-jsbench",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"description": "js-framework-benchmark fixture for Torpor (keyed @for) - drives 1000-row create/update/swap/select/remove ops.",
	"scripts": {
		"dev": "vite --host --port 5283 --strictPort",
		"build": "vite build",
		"preview": "vite preview --port 5283 --strictPort"
	},
	"dependencies": {
		"@torpor/view": "workspace:*"
	},
	"devDependencies": {
		"@torpor/unplugin": "workspace:*",
		"vite": "catalog:default"
	}
}
