{
  "name": "ripple-jsbench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "js-framework-benchmark fixture for ripple (keyed) - ported from frameworks/keyed/ripple to current ripple syntax; the 1000-row create/update/swap/select/remove ops.",
  "scripts": {
    "dev": "vp dev --host --port 5178 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5178 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default",
    "vite-plus": "catalog:"
  }
}
