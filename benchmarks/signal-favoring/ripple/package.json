{
  "name": "ripple-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.2",
  "description": "signal-favoring bench fixture for ripple - 100-component chain with track-based counters every 10 levels.",
  "scripts": {
    "dev": "vp dev --host --port 5193 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5193 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "vite": "catalog:default"
  }
}
