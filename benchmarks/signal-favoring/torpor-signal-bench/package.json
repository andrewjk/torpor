{
  "name": "torpor-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "signal-favoring bench fixture for Torpor - 100-component linear chain with stateful counters every 10th node. Matches the shared shape/contract in ../README.md.",
  "scripts": {
    "dev": "vp dev --host --port 5283 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5283 --strictPort"
  },
  "dependencies": {
    "@torpor/view": "workspace:*"
  },
  "devDependencies": {
    "@torpor/unplugin": "workspace:*",
    "vite": "catalog:default",
    "vite-plus": "catalog:"
  }
}
