{
  "name": "preact-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "signal-favoring benchmark fixture for Preact hooks.",
  "scripts": {
    "dev": "vp dev --host --port 5265 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5265 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
