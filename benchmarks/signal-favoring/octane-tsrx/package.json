{
  "name": "octane-tsrx-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "signal-favoring bench fixture for octane authored in .tsrx - 100-component chain with stateful counters every 10 levels.",
  "scripts": {
    "dev": "vp dev --host --port 5190 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5190 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
