{
  "name": "react-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "signal-favoring bench fixture for React 19 - 100-component chain; setState re-renders the owning component and cascades through descendants.",
  "scripts": {
    "dev": "vp dev --host --port 5192 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5192 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default"
  }
}
