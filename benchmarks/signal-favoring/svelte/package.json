{
  "name": "svelte-signal-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "signal-favoring benchmark fixture for Svelte 5 runes.",
  "scripts": {
    "dev": "vp dev --host --port 5276 --strictPort",
    "build": "vp build",
    "check": "svelte-check --tsconfig ./jsconfig.json",
    "preview": "vp preview --port 5276 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:default"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:default",
    "svelte-check": "catalog:default",
    "vite": "catalog:default"
  }
}
