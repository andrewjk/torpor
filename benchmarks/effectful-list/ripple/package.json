{
  "name": "ripple-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list bench fixture for ripple - 1000 rows with effect()/ref-cleanup lifecycle counters; exposes window.__mount/__op* + __fx for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5205 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5205 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
