{
  "name": "preact-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list benchmark fixture for Preact.",
  "scripts": {
    "dev": "vp dev --host --port 5266 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5266 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
