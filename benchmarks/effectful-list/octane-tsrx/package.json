{
  "name": "octane-tsrx-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list bench fixture for octane authored in .tsrx - 1000 cross-module rows each carrying useEffect/useLayoutEffect/shared-callback-ref; exposes window.__mount/__op* + __fx counters for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5201 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5201 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
