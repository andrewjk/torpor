{
  "name": "react-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list bench fixture for React 19 - 1000 cross-module rows each carrying useEffect/useLayoutEffect/shared-callback-ref; exposes window.__mount/__op* + __fx counters for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5203 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5203 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
