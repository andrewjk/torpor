{
  "name": "svelte-effectful-list-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "effectful-list benchmark fixture for Svelte 5.",
  "scripts": {
    "dev": "vp dev --host --port 5277 --strictPort",
    "build": "vp build",
    "check": "svelte-check --tsconfig ./jsconfig.json",
    "preview": "vp preview --port 5277 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:default"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:default",
    "svelte-check": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
