{
  "name": "react-compiler-memowall-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "memo-wall benchmark fixture for React 19 compiled with the production React Compiler; shares the vanilla React source so only the compiler differs.",
  "scripts": {
    "dev": "vp dev --host --port 5226 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5226 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@babel/core": "catalog:default",
    "@rolldown/plugin-babel": "0.2.3",
    "@vitejs/plugin-react": "catalog:default",
    "babel-plugin-react-compiler": "1.0.0",
    "terser": "catalog:default",
    "vite": "catalog:default"
  }
}
