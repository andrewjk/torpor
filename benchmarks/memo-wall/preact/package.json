{
  "name": "preact-memowall-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "memo-wall fixture for Preact - two 1000-row memo(Row) walls with exact render-count gates.",
  "scripts": {
    "dev": "vp dev --host --port 5267 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5267 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
