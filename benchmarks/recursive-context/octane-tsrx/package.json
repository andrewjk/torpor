{
  "name": "octane-tsrx-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "recursive-context bench fixture for octane authored in .tsrx - exposes window.__mount/__update/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5185 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5185 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default"
  }
}
