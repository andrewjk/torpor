{
  "name": "torpor-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "recursive-context bench fixture for Torpor - balanced binary tree (depth 10, 1024 leaves) with context reads at every leaf. Matches the shared shape/context contract in ../README.md.",
  "scripts": {
    "dev": "vp dev --host --port 5283 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5283 --strictPort"
  },
  "dependencies": {
    "@torpor/view": "workspace:*"
  },
  "devDependencies": {
    "@torpor/unplugin": "workspace:*",
    "vite": "catalog:default",
    "vite-plus": "catalog:"
  }
}
