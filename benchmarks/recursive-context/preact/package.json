{
  "name": "preact-recursive-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "recursive-context benchmark fixture for Preact.",
  "scripts": {
    "dev": "vp dev --host --port 5264 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5264 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
