{
  "name": "react-dbmon-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "dbmon update-stress bench fixture for React 19 - exposes window.__mount/__tick/__tickPartial/__remount/__sort/__unmount/__reset for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5198 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5198 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default"
  }
}
