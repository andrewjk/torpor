{
  "name": "preact-dbmon-bench",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "dbmon update-stress benchmark fixture for Preact.",
  "scripts": {
    "dev": "vp dev --host --port 5263 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5263 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
