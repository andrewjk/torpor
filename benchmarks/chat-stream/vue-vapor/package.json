{
  "name": "vue-vapor-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for Vue Vapor (3.6 vapor mode) - the Vue column for the deterministic token-stream ops.",
  "scripts": {
    "dev": "vp dev --host --port 5254 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5254 --strictPort"
  },
  "dependencies": {
    "vue": "catalog:default",
    "@vue/runtime-dom": "catalog:default",
    "@vue/runtime-vapor": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "catalog:default",
    "vite": "catalog:default"
  }
}
