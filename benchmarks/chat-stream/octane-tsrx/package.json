{
  "name": "octane-tsrx-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for octane authored in .tsrx - deterministic token-stream drain, long-history append, conversation switch, controlled composer.",
  "scripts": {
    "dev": "vp dev --host --port 5250 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5250 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
