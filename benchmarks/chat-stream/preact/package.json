{
  "name": "preact-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for Preact - the Preact column for the deterministic token-stream ops.",
  "scripts": {
    "dev": "vp dev --host --port 5262 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5262 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
