{
  "name": "react-chat-stream",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Streaming-chat fixture for React 19 - the React column for the deterministic token-stream ops.",
  "scripts": {
    "dev": "vp dev --host --port 5251 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5251 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
