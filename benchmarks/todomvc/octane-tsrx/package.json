{
  "name": "octane-tsrx-todomvc",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "TodoMVC fixture for octane authored in .tsrx - the canonical app-shaped benchmark (add/toggle/edit/filter/destroy scripted ops + app-code bundle size).",
  "scripts": {
    "dev": "vp dev --host --port 5240 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5240 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
