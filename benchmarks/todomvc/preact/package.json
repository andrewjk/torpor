{
  "name": "preact-todomvc",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "TodoMVC fixture for Preact - the Preact column for the app-shaped scripted ops and app-code bundle size.",
  "scripts": {
    "dev": "vp dev --host --port 5261 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5261 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
