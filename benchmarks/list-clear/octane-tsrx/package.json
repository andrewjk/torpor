{
  "name": "octane-tsrx-list-clear-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "list-clear bench fixture: one keyed list owning its parent and one sharing it with interleaved siblings; exposes window.__mount/__fill*/__op* for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5298 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5298 --strictPort"
  },
  "dependencies": {
    "octane": "catalog:default"
  },
  "devDependencies": {
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
