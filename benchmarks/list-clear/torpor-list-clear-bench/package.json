{
  "name": "torpor-list-clear-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "list-clear bench fixture for Torpor - keyed list bulk clear split by parent shape (shared lead/tail siblings vs sole child). Matches the shared DOM contract in ../README.md.",
  "scripts": {
    "dev": "vp dev --host --port 5283 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5283 --strictPort"
  },
  "dependencies": {
    "@torpor/view": "workspace:*"
  },
  "devDependencies": {
    "@torpor/unplugin": "workspace:*",
    "vite": "catalog:default",
    "vite-plus": "catalog:"
  }
}
