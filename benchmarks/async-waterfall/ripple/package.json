{
  "name": "ripple-async-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "async-waterfall fixture for ripple - 10 nested levels, each a tracked value filled by an effect-driven fetch; parallel by model, fine-grained updates with no re-render cascade.",
  "scripts": {
    "dev": "vp dev --host --port 5219 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5219 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "vite": "catalog:default"
  }
}
