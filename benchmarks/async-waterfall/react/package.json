{
  "name": "react-async-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "async-waterfall fixture for React 19 - 10 nested levels of use(fetch) under Suspense, init + startTransition-driven update; the reference waterfall column.",
  "scripts": {
    "dev": "vp dev --host --port 5217 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5217 --strictPort"
  },
  "dependencies": {
    "react": "catalog:default",
    "react-dom": "catalog:default"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "catalog:default",
    "vite": "catalog:default"
  }
}
