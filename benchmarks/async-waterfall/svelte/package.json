{
  "name": "svelte-async-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "async-waterfall fixture for Svelte 5 - nested stable await blocks that start independent work in parallel.",
  "scripts": {
    "dev": "vp dev --host --port 5280 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5280 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:default"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:default",
    "vite": "catalog:default"
  }
}
