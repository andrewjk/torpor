{
  "name": "preact-async-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "async-waterfall fixture for Preact - nested cached Suspense resources driven through init and update.",
  "scripts": {
    "dev": "vp dev --host --port 5269 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5269 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
