{
  "name": "torpor-async-composition-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "async-composition bench fixture for Torpor - dashboard with 8 versioned async resources via $await getters (stale-while-revalidate keeps old values during the version-bump transition). Matches the shared window contract in ../README.md.",
  "scripts": {
    "dev": "vp dev --host --port 5283 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5283 --strictPort"
  },
  "dependencies": {
    "@torpor/view": "workspace:*"
  },
  "devDependencies": {
    "@torpor/unplugin": "workspace:*",
    "vite": "catalog:default",
    "vite-plus": "catalog:"
  }
}
