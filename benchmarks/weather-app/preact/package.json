{
  "name": "preact-weather-app-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Preact reference weather-app benchmark fixture.",
  "scripts": {
    "dev": "vp dev --host --port 5294 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5294 --strictPort"
  },
  "dependencies": {
    "preact": "catalog:default"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:default",
    "vite": "catalog:default"
  }
}
