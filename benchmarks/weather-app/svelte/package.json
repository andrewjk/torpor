{
  "name": "svelte-weather-app-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Svelte 5 reference weather-app benchmark fixture.",
  "scripts": {
    "dev": "vp dev --host --port 5296 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5296 --strictPort"
  },
  "dependencies": {
    "svelte": "catalog:default"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "catalog:default",
    "vite": "catalog:default"
  }
}
