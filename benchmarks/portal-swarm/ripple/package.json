{
  "name": "ripple-portal-swarm-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "portal-swarm bench fixture for Ripple - 200 items each conditionally portalling a tooltip via ripple's <Portal>. Exposes window.__mount/__openAll/__rerenderA/... for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5224 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5224 --strictPort"
  },
  "dependencies": {
    "ripple": "catalog:default"
  },
  "devDependencies": {
    "@ripple-ts/vite-plugin": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
