{
  "name": "solid-portal-swarm-bench",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "portal-swarm bench fixture for Solid 2.0 - 200 items each conditionally portalling a tooltip via <Portal> from @solidjs/web. Exposes window.__mount/__openAll/__rerenderA/... for the Playwright harness.",
  "scripts": {
    "dev": "vp dev --host --port 5212 --strictPort",
    "build": "vp build",
    "preview": "vp preview --port 5212 --strictPort"
  },
  "dependencies": {
    "solid-js": "catalog:default",
    "@solidjs/web": "catalog:default"
  },
  "devDependencies": {
    "vite-plugin-solid": "catalog:default",
    "vite": "catalog:default",
    "terser": "catalog:default"
  }
}
